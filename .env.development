# Postgres connection settings
# Copy to .env and fill in your values.
PGHOST=localhost
PGPORT=5432
PGDATABASE=graphql_db
PGUSER=postgres
PGPASSWORD=postgres

# Or use a single connection string instead of individual fields:
DATABASE_URL=postgres://postgres:postgres@localhost:5432/graphql_db
